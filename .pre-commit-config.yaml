repos:
  - repo: local
    hooks:
      - id: systemd-unit-verify
        name: Validate systemd unit files
        entry: systemd-analyze verify --user
        language: system
        files: ^systemd/user/.*\.(service|conf)$
